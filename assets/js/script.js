"use strict";

jQuery(function ($) {
  // この中であればWordpressでも「$」が使用可能になる
  // setting
  var swiper = new Swiper('.swiper', {
    autoplay: {
      delay: 1000
    },
    loop: true
  });
  // setting
});